# TASKS
GET           /tasks         controllers.Tasks.list()
POST          /tasks         controllers.Tasks.create()
PUT           /tasks         controllers.Tasks.update()
DELETE        /tasks         controllers.Tasks.update()

# USERS
POST          /users         controllers.Users.create()

# TOKENS
POST          /tokens        controllers.Tokens.create()
